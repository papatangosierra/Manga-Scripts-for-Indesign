function exportCurrentPageForBlues() {    var myExportPreset = app.pdfExportPresets.item('Kodansha') // Select "Kodansha" PDF export preset    var myCurrentPage = app.activeWindow.activePage.name    app.pdfExportPreferences.pageRange = myCurrentPage; // select current page    if (app.activeDocument.documentPreferences.pageBinding == PageBindingOptions.leftToRight) { // If the book is bound left-to-right, it's also going to be lettered bottom-to-top, which means we can take the document page number to be the sequence number        app.activeDocument.exportFile(ExportFormat.PDF_TYPE, File(app.activeDocument.filePath + '/' + app.activeDocument.filePath.displayName + "_seq " + myCurrentPage), false, myExportPreset); // export current page with sequence number appended to filename    }    else { // if the book is bound right-to-left, it's lettered top-to-bottom, which means we need to derive the sequence number for file naming        var sequenceNumber = (app.activeDocument.pages.length + 1) - parseInt(myCurrentPage); // The last page in the document would be sequence 1        app.activeDocument.exportFile(ExportFormat.PDF_TYPE, File(app.activeDocument.filePath + '/' + app.activeDocument.filePath.displayName + "_seq " + sequenceNumber.toString()), false, myExportPreset); // export current page with sequence number appended to filename    }   }exportCurrentPageForBlues()